function transportFee(shift) {
    if (shift === 'morning'){
        return 'R20';
    }
function transportFee(shifty) {
    } if (shift === 'afternoon') {
        return 'R10';
    } else if (shift === 'nightshift') {
        return 'free';
    }
}